<h1>Music</h1>
{{#each musicList}}
  <p><b>Name: {{this.name}}</b> </p>
  <figure>
    <audio controls src="{{this.preview_url}}">
      Your browser does not support the
      <code>audio</code>
      element.
    </audio>
  </figure>

{{/each}}